services:
  scraper:
    build: .
    container_name: scholarship-scraper
    environment:
      - PROXY_URL=${PROXY_URL:-}
      - HEADLESS=true
      - TZ=UTC
    volumes:
      - ./docs:/app/docs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
